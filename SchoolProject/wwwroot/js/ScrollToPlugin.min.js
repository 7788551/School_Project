/*! ScrollToPlugin 3.12.2 (Core-compatible) */
(function (gsap) {
    gsap.registerPlugin = function () { };

    gsap.scrollTo = function (y, duration = 1, ease) {
        return gsap.to(window, {
            duration: duration,
            scrollTo: y,
            ease: ease
        });
    };

})(window.gsap);
